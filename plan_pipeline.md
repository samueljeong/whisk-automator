# Flow 자동화 파이프라인 개선 계획

## 목표
현재 "1개 제출 → 생성 대기 → 다운로드" 순차 방식을
"N개 제출 → 생성 대기 → N개 다운로드" 파이프라인으로 변경.

100개 이미지 기준: ~55분 → ~15분 이하 목표.

---

## Phase 1: 즉시 수정 (버그 픽스)

- [x] **1-1. STALL_TIMEOUT 버그 수정**
  - `almostDone`이 0개에서도 true → `Math.max(1, batchCount - 1)` 적용
  - 이것만으로 현재 "30초 타임아웃 → 재시도 → 중복 생성" 문제 해결

- [x] **1-2. Phase 3 에셋 보정**
  - `clickGenerate()` 직후 모든 이미지를 `assetSrcs`에 등록
  - 에셋 이미지가 생성 이미지로 오인되는 문제 방지

---

## Phase 2: BATCH_SIZE 복원 (핵심 변경)

현재 `BATCH_SIZE = 1`을 다시 키워서, 여러 프롬프트를 한번에 제출.

- [ ] **2-1. BATCH_SIZE를 설정 가능하게**
  - 기본값: 5 (한 번에 5개 프롬프트 제출 → 5개 생성 대기 → 5개 다운로드)
  - popup.html에 설정 UI 추가 (또는 기본값으로 시작)

- [ ] **2-2. 에셋 그룹핑 (같은 에셋 = 같은 배치)**
  - 현재 `promptsWithCharacters`는 에셋별로 정렬됨
  - 같은 에셋 조합(예: [뇌황] 5개)을 하나의 배치로 묶으면 에셋 선택 1회로 5개 제출 가능
  - 다른 에셋 조합이 섞이면 매번 에셋 재선택 필요 → 배치 경계로 분리

- [ ] **2-3. Phase 3 폴링 개선**
  - 5개 제출 후 5개 이미지가 나타날 때까지 폴링
  - maxWait 늘리기: 5개 × 60초 = 300초 (5분)
  - STALL_TIMEOUT: 이미 수정된 로직으로 N-1개 이상 나오면 30초 대기 후 진행

- [ ] **2-4. 다운로드 매칭 개선**
  - 현재 텍스트 매칭 + 위치 폴백이 불안정
  - 개선안: 프롬프트 제출 순서를 기록하고, 이미지 출현 순서(시간)로 매칭
  - 또는: 각 프롬프트 제출 직후 스냅샷 → 나중에 이미지-프롬프트 대응

---

## Phase 3: 완전 파이프라인 (고급)

> Phase 2가 잘 동작하면 Phase 3는 선택적

- [x] **3-1. 제출과 다운로드 분리**
  ```
  [제출 스레드]
  프롬프트 1 제출 (1.8초) → 프롬프트 2 제출 (1.8초) → ... → 프롬프트 N 제출

  [다운로드 스레드] (백그라운드 폴링)
  새 이미지 감지 → 다운로드 → 다음 이미지 감지 → ...
  ```
  - 제출이 끝나면 바로 다음 배치 시작
  - 다운로드는 이미지가 나타나는 대로 처리

- [ ] **3-2. 스트리밍 다운로드**
  - 이미지가 1개씩 나타날 때마다 바로 다운로드 (배치 완료 대기 X)
  - 모든 이미지 다운로드 완료 확인 후 다음 배치

---

## 기대 효과

### Phase 2 (BATCH_SIZE=5) 기준:
```
현재:  100개 × 33초 = 3,300초 (55분)
변경:  20배치 × (9초 제출 + 60초 대기 + 5초 다운로드) = 1,480초 (25분)
```

### Phase 3 (파이프라인) 기준:
```
제출: 100개 × 1.8초 = 180초 (3분)
생성: 최대 60초 (마지막 배치 대기)
다운로드: 100개 × 0.5초 = 50초
총: ~5분 (스트리밍 다운로드 시)
```

---

## 리스크

| 리스크 | 대안 |
|--------|------|
| Flow가 동시 생성 수를 제한할 수 있음 | 배치 크기를 줄여서 테스트 (3→5→10) |
| 이미지-프롬프트 매칭이 어려워짐 | 텍스트 매칭 + 제출 순서 기록 |
| 에셋 전환 시 추가 시간 | 같은 에셋끼리 그룹핑 |
| Flow 서버 과부하 시 에러 | 배치 간 쿨다운 설정 |

---

## 작업 순서

```
Phase 1 (버그 픽스) ✅ → Phase 2 (BATCH_SIZE 복원) → Phase 3 (파이프라인, 선택)
```

**현재 위치: Phase 1 완료, Phase 2 계획 수립**

Phase 2부터는 `BATCH_SIZE`만 5로 바꾸고 테스트해보는 것으로 시작.
이미 배치 처리 코드가 있으므로 큰 수정 없이 가능.
