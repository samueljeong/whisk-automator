(() => { const r = []; r.push('=== File Inputs ==='); document.querySelectorAll('input[type="file"]').forEach((el, i) => { r.push(i + ' | class: ' + (el.parentElement?.className?.substring(0,60) || '') + ' | accept: ' + el.accept + ' | hidden: ' + el.hidden); }); r.push('=== Upload/Drop Areas ==='); document.querySelectorAll('[class*="upload"], [class*="drop"], [class*="slot"], [class*="subject"], [class*="style"], [class*="scene"]').forEach((el, i) => { r.push(i + ' | ' + el.tagName + ' | class: ' + (typeof el.className === 'string' ? el.className.substring(0,80) : '') + ' | role: ' + el.getAttribute('role')); }); r.push('=== Large Buttons ==='); document.querySelectorAll('button').forEach((btn, i) => { const rect = btn.getBoundingClientRect(); if (rect.width > 50 && rect.height > 50) { r.push(i + ' | class: ' + (btn.className?.substring(0,60) || '') + ' | ' + Math.round(rect.width) + 'x' + Math.round(rect.height) + ' | text: ' + (btn.textContent?.substring(0,30) || '')); } }); copy(r.join('\n')); console.log('done'); })();