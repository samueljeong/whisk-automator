(() => { var r = []; r.push('=== 240x240 BUTTON DIVS - CONTEXT ==='); var btns = document.querySelectorAll('div[role="button"].sc-98b5b1bb-0'); btns.forEach(function(el, i) { var parent = el.parentElement; var grandparent = parent?.parentElement; var prevSibling = el.previousElementSibling; var nextSibling = el.nextElementSibling; var nearText = ''; var walker = el; for (var j = 0; j < 3; j++) { walker = walker?.parentElement; if (walker) { var t = walker.querySelector('h1,h2,h3,h4,h5,h6,label,span,p'); if (t && t.textContent.trim()) { nearText = t.textContent.trim().substring(0, 40); break; } } } r.push(i + ' | parentClass: ' + (parent?.className || '').substring(0, 50) + ' | grandparentClass: ' + (grandparent?.className || '').substring(0, 50) + ' | nearText: ' + nearText + ' | img: ' + (el.querySelector('img')?.src || 'none').substring(0, 60)); }); r.push(''); r.push('=== TEXT ON PAGE (h1-h6, labels) ==='); document.querySelectorAll('h1,h2,h3,h4,h5,h6,label,[class*="title"],[class*="label"],[class*="heading"]').forEach(function(el, i) { var t = el.textContent?.trim(); if (t && t.length > 0 && t.length < 50) { r.push(i + ' | ' + el.tagName + ' | class: ' + (typeof el.className === 'string' ? el.className.substring(0,40) : '') + ' | text: ' + t); } }); r.push(''); r.push('=== SECTIONS/PANELS STRUCTURE ==='); document.querySelectorAll('div[role="presentation"]').forEach(function(el, i) { var rect = el.getBoundingClientRect(); var childBtns = el.querySelectorAll('div[role="button"]').length; var headings = []; el.querySelectorAll('h1,h2,h3,h4,h5,h6,span').forEach(function(h) { var t = h.textContent?.trim(); if (t && t.length > 1 && t.length < 30 && headings.indexOf(t) === -1) headings.push(t); }); if (headings.length > 0 || childBtns > 0) { r.push(i + ' | ' + Math.round(rect.width) + 'x' + Math.round(rect.height) + ' | buttons: ' + childBtns + ' | headings: ' + headings.slice(0, 5).join(', ')); } }); copy(r.join('\n')); console.log('done - paste with Cmd+V'); })();