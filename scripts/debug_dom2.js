(() => { var r = []; r.push('=== ALL INPUTS ==='); document.querySelectorAll('input').forEach(function(el, i) { r.push(i + ' | type:' + el.type + ' | class:' + (el.className||'').substring(0,40) + ' | accept:' + el.accept + ' | hidden:' + el.hidden + ' | parent:' + (el.parentElement?.tagName||'')); }); r.push('=== ALL ELEMENTS WITH ARIA-LABEL ==='); document.querySelectorAll('[aria-label]').forEach(function(el, i) { var rect = el.getBoundingClientRect(); if (rect.width > 30) { r.push(i + ' | ' + el.tagName + ' | aria:' + el.getAttribute('aria-label').substring(0,50) + ' | ' + Math.round(rect.width) + 'x' + Math.round(rect.height)); } }); r.push('=== DIVS WITH ROLE ==='); document.querySelectorAll('[role]').forEach(function(el, i) { var rect = el.getBoundingClientRect(); if (rect.width > 80) { r.push(i + ' | ' + el.tagName + ' | role:' + el.getAttribute('role') + ' | class:' + (typeof el.className === 'string' ? el.className.substring(0,50) : '') + ' | ' + Math.round(rect.width) + 'x' + Math.round(rect.height)); } }); r.push('=== IMG TAGS ==='); document.querySelectorAll('img').forEach(function(el, i) { r.push(i + ' | src:' + (el.src||'').substring(0,60) + ' | ' + el.width + 'x' + el.height + ' | alt:' + (el.alt||'').substring(0,30)); }); copy(r.join('\n')); console.log('done - paste with Cmd+V'); })();