# Flow 자동화 파이프라인 리서치

## 현재 구조: 순차 처리 (BATCH_SIZE=1)

```
프롬프트 1개 제출 (~1.8초)
  → 생성 대기 (~30-60초)
    → 다운로드 (~1초)
      → 다음 프롬프트...
```

**100개 이미지 × 33초 = ~55분**

## 발견된 핵심 사실들

### 1. Flow는 생성 중에도 새 프롬프트 제출 가능
- `clickGenerate()` 후 입력 필드가 비활성화되지 않음
- 생성 버튼도 계속 활성 상태
- 이전 세션에서 실수로 3개를 연속 제출했을 때 3개 모두 정상 생성됨

### 2. 생성된 이미지는 DOM에 계속 남음
- 배치 1 재시도가 2초 만에 성공 = 이전 시도의 이미지가 그대로 있었음
- Flow 갤러리에 시간순으로 누적됨

### 3. 현재 감지 방식의 한계
- `preGenSrcs` 스냅샷 기반 → 배치 시작 시점에만 캡처
- `assetSrcs` 등록 누락 → 에셋을 생성 이미지로 오인
- STALL_TIMEOUT 버그 → `batchCount=1`일 때 0개에서도 조기 종료 (수정 완료)

### 4. 타이밍 분석
- 프롬프트 제출: ~1.8초 (에셋 선택 + 텍스트 입력 + 생성 클릭)
- 이미지 생성: 10~60초 (Flow 서버 상태에 따라 변동)
- 다운로드: ~1초
- **병목**: 생성 대기 시간 (30-60초를 매번 기다림)

## STALL_TIMEOUT 버그 (수정 완료)

```js
// 수정 전: batchCount=1일 때 almostDone이 항상 true
var almostDone = detectedNewImages.length >= batchCount - 1; // 0 >= 0 = true!

// 수정 후: 최소 1개 이상 감지돼야 조기 종료
var almostDone = detectedNewImages.length >= Math.max(1, batchCount - 1);
```

이 버그 때문에 이미지가 30초 안에 안 나오면 무조건 실패로 처리됨.
실제로는 30-60초 걸리는 것이 정상인데, maxWait(90초)까지 기다리지 못하고 30초에서 잘림.

## Phase 3 에셋 보정 (추가 완료)

`clickGenerate()` 직후, Phase 3 폴링 시작 전에 현재 모든 이미지를 `assetSrcs`에 등록.
이 시점에는 생성된 이미지가 아직 없으므로 안전.
