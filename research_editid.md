# 리서치: 파이프라인 이미지-프롬프트 매칭 문제

## 현재 문제

### 증상
- 5개 제출 → 4개 다운로드 (1개 누락)
- 다운로드된 4개 중 3개 파일명 틀림

### 텍스트 매칭이 실패하는 근본 원인
모든 프롬프트가 **동일한 스타일 프리픽스**로 시작:
```
"고급 중화 무협 만화 화풍, 중원 무림 배경, 부드럽고 세밀한 선화에 굵은 외곽선, 풍부하..."
```
- `findPromptForImage()`가 80글자 매칭 → 프리픽스만 매칭됨
- 5개 프롬프트의 앞 80글자가 동일 → 순서대로 할당 (텍스트 매칭이 아님)
- 이미지 완성 순서 ≠ 제출 순서 → 파일명 밀림

### DOM 진단 결과 요약 (debug_card_dom 1~6)
1. **갤러리 이미지 DOM에 텍스트 없음** (textLen=0 at all depths)
2. 각 이미지에 부모 `<a>` 태그의 고유 edit URL 있음: `/edit/{EDIT_ID}`
3. React fiber에 `data-tile-id`, `src`, `href` 있지만 프롬프트 텍스트는 없음
4. `__NEXT_DATA__` trpcState에서도 프롬프트 못 찾음

## 해결 방안: editId 추적

### 핵심 아이디어
Phase 2에서 프롬프트 제출할 때마다, **제출 직후 새로 나타난 `<a>` 태그의 editId를 캡처**하여 `editId → promptIndex` 매핑을 만든다.

### 동작 원리
1. 생성 클릭 전: 현재 모든 `<a href="*/edit/*">` 스냅샷
2. 생성 클릭
3. 폴링 (최대 10초): 새 `<a>` 태그 출현 감지
4. 새 editId 발견 → `editIdMap[editId] = promptIndex` 기록
5. 다음 프롬프트로 이동

Phase 3에서:
1. 새 이미지 감지 (getMediaUrlRedirect + 크기 필터)
2. 이미지의 부모 `<a>` 태그에서 editId 추출
3. `editIdMap`에서 promptIndex 조회 → 정확한 파일명 할당

### 장점
- 텍스트 내용 의존 없음 (동일 프리픽스 문제 해결)
- 구조적 매칭이라 100% 정확
- Phase 2에서 추가 대기 시간 최소 (1~2초/프롬프트)

### 위험 요소
- 새 `<a>` 태그가 생성 클릭 직후 나타나지 않을 수 있음 → 최대 10초 폴링으로 대비
- editId가 없는 카드일 수 있음 → editId 매핑 실패 시 위치 폴백

## 수정 대상
- `popup/popup.js`: Phase 2 루프 + Phase 3 매칭 로직
- 단일 파일 수정
