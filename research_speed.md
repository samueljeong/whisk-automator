# 속도 병목 분석 (순차 모드)

## 현재 성능: 40장 = 30~40분 (이미지당 45~60초)

---

## 이미지 1개당 시간 분해 (평균 50초 기준)

| 단계 | 시간 | 비중 | 제어 가능? |
|------|------|------|-----------|
| **AI 생성** (Flow 서버) | ~25초 | 50% | ❌ |
| `selectAssetByName()` 하드코딩 sleep | 3.3초 | 6.6% | ✅ |
| `waitForGeneration()` 폴링 낭비 | 0~2초 | ~2% | ✅ |
| `fillPrompt()` sleep들 | 0.8초 | 1.6% | ✅ |
| `downloadImage()` 네트워크 | 1~2초 | 3% | ❌ |
| 프롬프트 간 딜레이 | 1초 | 2% | ✅ |
| **기타 sleep 합계** | ~18초 | 37% | ⚠️ |

---

## 핵심 발견: 시간의 ~50%가 불필요한 대기

### 1. `selectAssetByName()` — 매 프롬프트마다 3.3초 (줄 2164~2320)
```
sleep(800)  — "+" 버튼 클릭 후 대기
sleep(200)  — 검색 입력 포커스
sleep(200)  — 이전 텍스트 삭제
sleep(1000) — 검색 결과 대기 ← 가장 큰 낭비
sleep(300)  — ArrowDown 후
sleep(800)  — Enter 후 ← 두 번째 큰 낭비
합계: 3,300ms
```

### 2. `waitForGeneration()` — 폴링 간격 2초 (줄 2679)
- 이미지가 생성되더라도 최대 2초 뒤에야 감지
- 500ms로 줄이면 평균 750ms 절약

### 3. `fillPrompt()` — 불필요하게 긴 sleep (줄 1735~1820)
```
sleep(200)  — 포커스 후
sleep(100)  — 선택 전
sleep(300)  — 삽입 후
합계: ~800ms → 300ms로 축소 가능
```

### 4. 프롬프트 간 딜레이 — 1초 고정 (줄 3348)
- `sleep(Math.max(delayMs, 1000))` → 300ms로 축소 가능

---

## 하지만 — 진짜 해결책은 순차를 유지하면서 이런 작은 최적화가 아님

### 현실적 절약 효과
- 위 최적화 전부 적용: 이미지당 ~5초 절약
- 40장 기준: ~200초 (3.3분) 절약
- **30~40분 → 27~37분** — 체감 차이 미미

### 근본 문제
- AI 생성 25초는 줄일 수 없음
- 순차 모드에서는 이 25초 동안 아무것도 안 함
- **진짜 속도 향상 = 생성 대기 중에 다음 프롬프트 제출 (파이프라인)**

---

## 속도 향상 전략 비교

### A. 순차 최적화 (sleep 줄이기)
- 절약: 3~4분 / 위험: 낮음
- 결과: 27~37분 (체감 미미)

### B. 2개씩 제출 (미니 파이프라인)
- 2개 제출 → 2개 대기 → 2개 다운로드
- 절약: ~40% (대기 시간 겹침)
- 결과: 18~24분
- **문제: 파일명 매칭** — 2개 중 어느 게 먼저 완성될지 모름

### C. 제출과 대기를 겹치기 (비동기 파이프라인)
- 이미지 생성 대기하면서 다음 프롬프트 제출
- 절약: ~60%
- 결과: 12~16분
- **문제: 파일명 매칭** — 동일

### D. 텍스트 매칭으로 파일명 해결 + 파이프라인
- 각 프롬프트의 고유 키워드로 DOM에서 텍스트 매칭
- 파이프라인으로 빠르게 제출
- **Flow DOM 구조에 따라 가능 여부 결정**

---

## 파일명 매칭 가능성 재검토

이전에 FIFO가 실패한 이유: "완성 순서 ≠ 제출 순서"

하지만 다른 매칭 방법이 있을 수 있음:
1. **DOM 구조 매칭** — Flow가 프롬프트 텍스트를 이미지 카드에 표시하는지?
2. **이미지 메타데이터** — URL에 프롬프트 관련 ID가 포함되는지?
3. **제출 시 타임스탬프** — 제출 시각과 이미지 출현 시각 비교?
4. **카드 순서** — Flow DOM에서 카드의 순서가 일정한지?

→ 이것들을 확인하려면 **실제 Flow DOM을 분석해야 함**
